# 2026-09-11 — pre-Unity validation v001

Status: passed for the static scope; `ready-for-final-validation`.

Executed from the registered `codex/story-first-snow` worktree after merging
the current main baseline:

- standalone `inkjs@2.3.2` compilation and exhaustive traversal: 704 routes,
  ending distribution 288/320/96, zero compiler warnings/errors and zero
  runtime problems;
- story source-contract audit: nine locations, eight whole-character variants,
  protagonist aliases/default outfits, notification placement, branch-specific
  consequences, evolving star knowledge and ending-specific aftermath pass;
- `verify-pre-unity.cjs`: 47 ordered preview blocks, two referenced approved
  character PNGs, safe paths, exact source/speaker correspondence and archive
  date/hash checks pass;
- episode-cover audit: approved SHA-256, 1024×1536 geometry, source binding,
  current local/reference SDK support and three negative probes pass;
- JavaScript syntax checks, scoped `git diff --check`, archive evidence
  comparison and repository `docs-check` pass.

Deferred by contract: Unity import and serialization, canonical compiled Ink
JSON/source map, content build, browser layout inspection, catalog crop/display,
runtime route/visual acceptance and measured play time. No Unity process,
catalog mutation, commit, push or publication was started.

Known history limitation: the archive was adopted from surviving material on
2026-09-11 UTC; unavailable earlier candidates and user-facing exchanges were
not reconstructed.
