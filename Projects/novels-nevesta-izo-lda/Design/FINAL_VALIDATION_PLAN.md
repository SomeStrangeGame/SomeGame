# Final validation / acceptance plan

This plan must run only after a fresh human authorization under `UnityConcurrency.md`.

Precondition: retain current narrative/full-Ink originality evidence for the exact hashes recorded in `ORIGINALITY_REVIEW.md`. The WAV, Mira contact-sheet, alpha-proof and web-preview preparation blockers were resolved on 2026-09-11; the next gate starts with Unity.

1. Register/check `unity_novels_nevesta_izo_lda`, import the atomic project and preserve generated `.meta` files.
   Confirm the registered worktree SDK and preserve the staged `_catalogCover` field during import.
2. Compile root Ink and source map; run story validation/build for `editor` and required Android content.
3. Audit all five choice points and reachability of `ending_thaw`, `ending_glass`, `ending_lantern`; compare with the 38-route baseline and 16 pre-final menus in `Design/audit_story.py`. Measure compiled route text and require 3,000–5,000 words for every complete route. Replay ledger-first and confession `thaw`, village response variants, hidden shared-promise options for missing trust/ribbon, deliberate `glass`, and the prepared-letter sequence in `lantern`. Require all three final selections to visit `dawn_seal` before settlement; there must be no failed-shared forced return.
4. Register the story in the catalog only within acceptance scope, preserving existing order; rebuild catalog.
   Verify `s01e01` retains `_catalogCover: s01e01.png` after Ink metadata refresh and its source PNG is copied to the platform's `episode-covers` preview output. Inspect episode-card image loading before story payload download and after ready state, portrait cropping and UI contrast. In an isolated fixture, verify missing-own-cover fallback and priority: episode video → loaded own episode image → story video → story image; do not remove the production cover to simulate failure.
5. Build a fresh Android Embedded APK and replay the smallest path set covering all endings and semantically distinct branches; require no crash, ANR, error or `fallback.used`.
6. Observe actual Player states: long narrator line, named dialogue, both two-option and three-option final prompts, missing-icon fallback, narrow portrait viewport, the new letter-house/orchard/weir scenes, storm scene, alpha edges, character scale/gaze and Bubble contrast.
7. Record APK hash/build identity, emulator serial/API, runtime markers, exact choices/endings and screenshots for key visual scenes.

No catalog mutation, Unity process, build, emulator, publication or main integration was performed in the preparation stage.
